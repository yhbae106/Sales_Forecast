const CACHE='scm-sales-forecast-pwa-v3';
const SHELL=['/Sales_Forecast/','/Sales_Forecast/index.html','/Sales_Forecast/manifest.webmanifest','/Sales_Forecast/app-icon.svg'];
self.addEventListener('install',e=>{self.skipWaiting();e.waitUntil(caches.open(CACHE).then(c=>c.addAll(SHELL)).catch(()=>{}))});
self.addEventListener('activate',e=>{e.waitUntil((async()=>{for(const k of await caches.keys())if(k.startsWith('scm-sales-forecast-pwa-')&&k!==CACHE)await caches.delete(k);await self.clients.claim()})())});
async function cacheFirst(r){const hit=await caches.match(r);if(hit)return hit;const res=await fetch(r);if(res&&res.ok){const c=await caches.open(CACHE);c.put(r,res.clone()).catch(()=>{})}return res}
async function networkFirst(r){try{const res=await fetch(r,{cache:'no-store'});if(res&&res.ok){const c=await caches.open(CACHE);c.put(r,res.clone()).catch(()=>{})}return res}catch(err){return (await caches.match(r))||(await caches.match('/Sales_Forecast/'))}}
self.addEventListener('fetch',e=>{const r=e.request;if(r.method!=='GET')return;const u=new URL(r.url);if(u.origin!==location.origin||!u.pathname.startsWith('/Sales_Forecast/'))return;if(r.mode==='navigate'){e.respondWith(networkFirst(r));return}if(u.pathname.endsWith('/shared-data/sales-history.json')){e.respondWith(networkFirst(r));return}if(u.pathname.endsWith('.js')||u.pathname.endsWith('.css')||u.pathname.endsWith('.svg')||u.pathname.endsWith('.webmanifest')){e.respondWith(cacheFirst(r));return}e.respondWith(cacheFirst(r))});